{
  "name": "ts-results",
  "version": "1.1.0",
  "description": "",
  "scripts": {
    "build": "npm run clean && tsc && copyfiles README.md LICENSE src/package.json dist --flat",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.1.0",
    "rxjs": "^6.1.0",
    "typescript": "^3.3.3"
  }
}
